{
	"name": "Python 3",
	"dockerFile": "Dockerfile",
	"customizations": {
		"vscode": {
			"extensions": [
				"ms-python.debugpy",
				"ms-python.python",
				"mechatroner.rainbow-csv",
				"ms-azuretools.vscode-docker",
				"KevinRose.vsc-python-indent"
			]
		}
	},
	// "postCreateCommand": [
	// 	"/bin/bash",
	// 	"-c",
	// 	"curl -O https://s3.us-west-2.amazonaws.com/amazon-eks/1.29.0/2024-01-04/bin/linux/amd64/kubectl && chmod +x ./kubectl && mkdir -p $HOME/bin && cp ./kubectl $HOME/bin/kubectl && export PATH=$HOME/bin:$PATH"
	// ]
	
	
	// Features, ports, postCreateCommand, customizations, and remoteUser can be added here as needed.
}